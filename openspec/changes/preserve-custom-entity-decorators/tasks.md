## 1. 生成器重构

- [x] 1.1 重构 schema 生成流程，在写文件前先构建一份期望中的实体内存表示
- [x] 1.2 将文件写入拆分为“新建受管文件”和“patch 既有受管文件”两条路径
- [x] 1.3 明确并文档化生成器托管片段边界，确保除这些片段外默认保留用户现有代码

## 2. Patch 协调

- [x] 2.1 实现受管文件 patch 逻辑，定位目标实体类并就地更新生成器托管的类装饰器和生成属性
- [x] 2.2 在移除 Better Auth schema 中已不存在的过期生成属性时，保留手写 import、非 MikroORM 装饰器、方法、额外属性和注释
- [x] 2.3 为无法安全 patch 的受管文件增加显式错误，同时继续保护非受管文件不被覆盖

## 3. 验证

- [x] 3.1 为 schema generator 增加测试，验证重新生成后仍保留 `@nestjs/graphql`、`class-validator` 等手写装饰器和相关 import
- [x] 3.2 增加测试，验证手写方法、额外属性和注释在再生成后仍然保留
- [x] 3.3 增加测试，验证过期生成属性会被移除，且模糊的受管文件状态会以可读错误失败
- [x] 3.4 确保 patch 式再生成后的实体文件仍可通过 typecheck

## 4. 文档

- [x] 4.1 更新 README 和生成器文档，说明 patch 式再生成行为以及“默认保留用户代码、只更新托管片段”的边界
- [x] 4.2 文档化当前不支持的自定义模式，以及在再生成拒绝不安全合并时的处理方式
